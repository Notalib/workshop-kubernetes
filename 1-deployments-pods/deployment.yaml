# A Deployment manifest with gaps to fill in (TASK 8).
# Fill in every TODO, then:  kubectl apply -f deployment.yaml
#
# Docs: https://kubernetes.io/docs/concepts/workloads/controllers/deployment/

apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  labels:
    app: demo
spec:
  # TODO: how many identical Pods should run?
  replicas: # ___

  # The Deployment finds the Pods it owns by matching these labels.
  # They MUST match .spec.template.metadata.labels below.
  selector:
    matchLabels:
      app: demo

  # The Pod template: the blueprint every replica is stamped from.
  template:
    metadata:
      labels:
        app: # TODO: which label? (must match the selector above)
    spec:
      containers:
        - name: nginx
          # TODO: which image? Pin a tag, don't use :latest. Check https://hub.docker.com
          image: # ___
          ports:
            - containerPort: 80

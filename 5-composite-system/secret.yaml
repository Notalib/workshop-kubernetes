# The database password (TASK 1). Provided complete.
# stringData lets you write the plaintext value; Kubernetes base64-encodes it for you.
# (In a real setup this would not be committed to git — see Secrets discussion in module 2.)
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  password: db-wrz2z

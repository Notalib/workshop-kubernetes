# SOLUTION — completed Service in front of the `demo` Deployment (module 4, TASK 4).
apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: web-pod       # matches the Pods with label `app=web-pod`
  ports:
    - port: 80         # port the Service exposes
      targetPort: 80   # port on the Pod/container

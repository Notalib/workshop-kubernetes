# A Service in front of the `demo` Deployment (TASK 4). Fill in the TODOs.
apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  # The Service routes to Pods whose labels match this selector.
  # It must match the labels on your Deployment's Pods.
  selector:
    app: # TODO: which label? (see app.yaml)
  ports:
    - port: 80                # port the Service exposes
      targetPort: 80          # port on the Pod/container

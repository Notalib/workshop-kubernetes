# An Ingress routing localhost -> the `demo` Service (TASK 4). Fill in the TODOs.
# On Rancher Desktop / k3d the class is "traefik"; on kind with ingress-nginx use "nginx".
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo
spec:
  ingressClassName: # TODO: which ingress controller should handle this? (Rancher Desktop uses "traefik").
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: # TODO: name + port of the Service to route to (see service.yaml)
                port:
                  number: # TODO: Which port does the Service expose?

# edu — Python Jupyter notebook

This edu folder simply demonstrates how to create and expose the Python jupyter notebook for AI inference in a Kubernetes cluster.

All of the necessary manifests are defined in a single file `app.yaml`

## Install the app

```bash
kubectl apply -f app.yaml
```
